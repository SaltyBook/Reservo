<Window x:Class="Reservo.Views.FeedBack"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Reservo.Views"
        xmlns:vm="clr-namespace:Reservo.ViewModels"
        mc:Ignorable="d"
        Title="FeedBack" Height="300" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel Margin="10">
            <TextBlock Text="Betreff" />
            <TextBox Text="{Binding Subject, UpdateSourceTrigger=PropertyChanged}"
                     Height="20" />
        </StackPanel>
        
        <StackPanel Grid.Row="1"
                    Margin="10">
            <TextBlock Text="Nachricht" />
            <TextBox Text="{Binding Message, UpdateSourceTrigger=PropertyChanged}"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     Height="120" />
        </StackPanel>

        <Button Grid.Row="2"
                Content="Sende Nachricht"
                Height="25"
                Width="100"
                Margin="10"
                HorizontalAlignment="Center"
                VerticalAlignment="Bottom"
                Command="{Binding FeedBackCommand}" />
    </Grid>
</Window>
